name: Manual Notebook Smoke Test

on:
  push:
    branches:
      - refactor/notebook-testing
  workflow_dispatch:
    inputs:
      group:
        description: 'Notebook group to test'
        required: true
        type: choice
        options:
          - all
          - data-access
          - analysis
          - climate-profiles
          - collaborative

jobs:
  run-notebooks:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/eagle-rock-analytics/cae-notebook:2026.03.03
      options: --user root
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4

      - name: Install papermill
        run: pip install papermill

      - name: Run notebooks
        run: |
          group="${{ github.event.inputs.group }}"
          if [ "$group" = "all" ]; then
            dirs="data-access analysis climate-profiles collaborative"
          elif [ -z "$group" ]; then
            dirs="data-access"
          else
            dirs="$group"
          fi

          failed=""
          for dir in $dirs; do
            while IFS= read -r notebook; do
              echo "::group::$notebook"
              if papermill "$notebook" /dev/null --no-progress-bar; then
                echo "PASSED: $notebook"
              else
                echo "FAILED: $notebook"
                failed="$failed\n$notebook"
              fi
              echo "::endgroup::"
            done < <(find "$dir" -name "*.ipynb" | sort)
          done

          if [ -n "$failed" ]; then
            echo -e "\nFailed notebooks:\n$failed"
            exit 1
          fi
          echo "All notebooks passed!"
